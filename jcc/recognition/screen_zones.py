"""屏幕区域定义 — 基于 1280x720 分辨率

所有坐标为 (x, y, w, h) 格式，相对于全屏左上角。
实际坐标需要在模拟器上标定校准。
"""

# ---------- 自己的信息 ----------

# 金币数字区域
GOLD = (872, 680, 60, 28)

# 等级数字区域
LEVEL = (312, 680, 30, 28)

# 经验条区域
XP = (340, 680, 80, 28)

# 血量区域
HP = (120, 10, 50, 24)

# 阶段文字 如 "3-2"
STAGE = (600, 8, 80, 24)

# 商店 5 个棋子槽位（从左到右）
SHOP_SLOTS = [
    (372, 665, 100, 50),
    (482, 665, 100, 50),
    (592, 665, 100, 50),
    (702, 665, 100, 50),
    (812, 665, 100, 50),
]

# 备战席 9 个槽位
BENCH_SLOTS = [
    (68 + i * 120, 590, 100, 60) for i in range(9)
]

# 棋盘区域（整体）
BOARD = (200, 180, 880, 380)

# ---------- 对手头像列表（右侧） ----------

# 对手头像位置（最多 7 个对手），用于点击切换视角
OPPONENT_PORTRAITS = [
    (1240, 82 + i * 72, 36, 36) for i in range(7)
]

# ---------- 对手信息（侦查视角下） ----------

# 侦查时对手的等级/血量显示在同一位置
SCOUT_HP = HP
SCOUT_LEVEL = LEVEL
SCOUT_BOARD = BOARD
